<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tkfc.boot.starter.mybatis.extend.BaseMapper">
    <!--
        通用 BaseMapper XML 配置
        用于解决 Spring Boot 3 中 Provider 注解导致的 factoryBeanObjectType 类型错误

        注意：这个文件只是为了让 MyBatis 能够正确识别 BaseMapper 的类型
        实际的 SQL 生成仍然由 BaseSqlProvider 处理
    -->
</mapper>
